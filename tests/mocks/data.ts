import {Data} from "../../src"

const mockData: Data = {
    string: "one",
    strings: ["one", "two", "three"],
    number: 1,
    numbers: [1, 2, 3],
    boolean: true,
    booleans: [true, false, true],
}

export {mockData}
